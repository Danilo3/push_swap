//
// Created by Aletha Yellin on 26/02/2020.
//
#include "push_swap.h"

int		parse_values(t_app * app, char *arg, int *value)
{
	if check_dup(stack_a) || !check_commands(stack_a, stack_b))
	{
		free_stack(stack_a);
		free_stack(stack_b);
	}
}

int		parse_args(t_app *app, char *arg, int *value)
{


}